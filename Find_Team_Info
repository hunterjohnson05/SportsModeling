def team_info(team_name):
    from nba_api.stats.static import teams
    teams = teams.get_teams()
    specific_team = [x for x in teams if x['full_name'] == team_name][0]
    return(specific_team)
team_info("Miami Heat")
